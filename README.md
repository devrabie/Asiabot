# Asiabot 🇮🇶

بوت تيليجرام لإدارة حسابات آسياسيل (Asiacell) العراق. يتيح البوت تسجيل الدخول بحسابات متعددة، مراقبة الرصيد، وتحديث الرموز (Tokens) تلقائيًا للحفاظ على الجلسة نشطة.

## المميزات ✨

*   **إدارة حسابات متعددة:** ربط أكثر من رقم آسياسيل بنفس حساب التيليجرام.
*   **مراقبة الرصيد:** التحقق من الرصيد كل 30 دقيقة وإرسال إشعارات عند زيادة أو نقصان الرصيد.
*   **التحديث التلقائي:** تحديث رموز الدخول (Access Tokens) تلقائيًا كل 11 ساعة لضمان عدم انتهاء الجلسة.
*   **قاعدة بيانات:** تخزين البيانات بشكل آمن باستخدام SQLite.
*   **تسجيل الدخول:** عملية تسجيل دخول آمنة عبر OTP.

## المتطلبات 📋

*   Python 3.11 أو أحدث.
*   حساب تيليجرام وبوت توكن (من @BotFather).

## التثبيت والتشغيل 🚀

1.  **استنساخ المستودع:**
    ```bash
    git clone https://github.com/yourusername/asiabot.git
    cd asiabot
    ```

2.  **إنشاء بيئة افتراضية (اختياري ولكن مستحسن):**
    ```bash
    python -m venv venv
    source venv/bin/activate  # في Linux/Mac
    # أو
    venv\Scripts\activate     # في Windows
    ```

3.  **تثبيت المكاتب المطلوبة:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **إعداد المتغيرات البيئية:**
    *   قم بإنشاء ملف باسم `.env` في المجلد الرئيسي.
    *   أضف التوكن الخاص بالبوت:
        ```env
        BOT_TOKEN=your_telegram_bot_token_here
        ```

5.  **تشغيل البوت:**
    *   تأكد من أنك في المجلد الرئيسي للمشروع.
    *   قم بتشغيل الأمر التالي:
        ```bash
        export PYTHONPATH=$PYTHONPATH:$(pwd)
        python src/bot/main.py
        ```

## هيكلية المشروع ays

*   `src/api`: التعامل مع واجهة برمجة تطبيقات آسياسيل.
*   `src/bot`: كود البوت ومعالجة الأوامر (Handlers).
*   `src/database`: إدارة قاعدة البيانات (SQLite).
*   `src/services`: الخدمات الخلفية (المجدولة).
*   `data/`: مجلد لتخزين قاعدة البيانات وملف البروكسي (`proxies.txt` إذا لزم الأمر).

## الاستخدام 📱

1.  ابدأ المحادثة مع البوت باستخدام الأمر `/start`.
2.  أضف حسابًا جديدًا باستخدام الأمر `/add_account`.
3.  سيطلب منك البوت إدخال رقم الهاتف (077xxxxxxxx).
4.  سيصلك رمز تحقق (OTP) على هاتفك، قم بإرساله للبوت.
5.  بمجرد تسجيل الدخول بنجاح، سيقوم البوت بحفظ الجلسة وبدء مراقبة الرصيد.

## ملاحظات ⚠️

*   هذا البوت يعتمد على واجهة برمجة تطبيقات غير رسمية، وقد تتوقف عن العمل إذا قامت الشركة بتغيير نظامها.
*   تأكد من حماية ملف `.env` وعدم مشاركته.
